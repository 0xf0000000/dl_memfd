dl_memfd carrega o .so direto na RAM via arquivo fantasma, evitando gravação no disco e dificultando a detecção por varreduras de arquivos.




dl_memfd loads the .so file directly into RAM via a phantom file, avoiding disk writing and making it harder to detect by file scans.
